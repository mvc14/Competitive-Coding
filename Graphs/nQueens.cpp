#include <bits/stdc++.h>
using namespace std;

// to be done later; backtracking still to be worked on! 
class Solution {
public:
    vector<vector<string>> solveNQueens(int n) {
        vector<vector<string>> res(n);
        for (int i = 0; i < n; i++) {
            vector<string> tmp(n);

        }
    }

    void back_tracking(int n) {

    }
};