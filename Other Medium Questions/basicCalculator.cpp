#include <string>
#include <cctype>
#include <stack>
using namespace std;

class basicCalculator {
public:
    int calculate(string s) {
        stack<int> nums;
        stack<char> ops;
        
    }
};
